export function Compras() {
    // const userToken = JSON.parse(window.localStorage.getItem("usuarioLogueadoPurificadora"));

    return (
        <section style={{height: "calc(100vh - 140px)"}}>
            <h2>Bienvenido al área de compras</h2>

        </section>
    );
}